# Next Steps (Claude Execution Plan)

## Phase 2: Build the trunk + branches (you already have leaves)
1) Decide the **canonical pillar URLs** and ensure each pillar exists as a real page.
2) For each pillar, create **20–50 cluster pages** (branches).
3) For each existing leaf page, assign:
   - pillar_slug
   - cluster_slug
   Then enforce links:
   - leaf → cluster
   - leaf → pillar
   - cluster → pillar
   - cluster ↔ related clusters
4) Add the "Friendly Intelligence" block to page templates sitewide.
5) As traffic data appears, build clusters around what gets impressions first (Problem Radar behavior).

## Non-negotiables
- Calm, friendly tone
- Clear hierarchy
- Strong internal linking
- Text PJ on every page
