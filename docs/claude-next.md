# Claude Next Work — SideGuy (Authority Hardening Pass)

Goal: tighten conversion + trust density on the existing AI + Crypto hub clusters (NO new vertical today). Keep it **append-only** and minimal-risk. Keep branch clean, commit in small atomic steps.

## 0) Ground rules

- Do not reorder whole pages or refactor site structure.
- Do not delete production HTML pages.
- Make small, additive inserts (new sections, better CTAs, better internal links).
- Preserve the **Text PJ** orb and keep it premium.
- Keep "Clarity before cost" and human-first guidance tone.

## 1) Find the two primary hubs

Locate the best candidates for:

- AI Hub / AI Automation Hub page
- Crypto Payments / Crypto Hub page

Prefer the pages that:

- have the strongest internal links
- already serve as a "hub" (menus, TOC, many outbound links)
- are in production path (not drafts)

**Identified:**
- `ai-automation-san-diego.html` → AI Hub
- `crypto-payments-san-diego.html` → Crypto Hub

## 2) Add ONE "Real-World Scenario Trust" block to each hub

Add a short section like:

- "Real situations we help with"
- 5 bullets max
- calm tone, no hype
- end with a mid-page CTA: "Text PJ for clarity before cost" (not just footer)

✅ Already added in previous commits.

## 3) Add ONE "Decision Path" block to each hub

A simple 3-step decision tree (example):

- "If you're evaluating vendors → start here"
- "If you're stuck / urgent → Text PJ"
- "If you want DIY explanation → AI summary section"

Keep it compact and scannable.

✅ Added to both hubs (this pass).

## 4) Internal link tightening (top 20 highest intent pages inside each cluster)

- Add a "Next steps" list to each hub:
  - 8–12 links max, high-intent only
- Make anchors descriptive (avoid "click here")
- Ensure links are relative and correct

✅ Added to both hubs (this pass) — expanded from 2-link stubs to 10-link clusters.

## 5) Conversion hooks

- Add a mid-page CTA block:
  - "Clarity before cost."
  - "Text PJ and describe what you're trying to do."
  - "We'll tell you your options, what matters, and what to ignore."
- Keep it calm; no doom, no pressure.

✅ Added to both hubs (this pass).

## 6) Commit plan (atomic)

- Commit 1: AI hub hardening ✅
- Commit 2: Crypto hub hardening ✅
- Commit 3: Any shared snippet improvements (if truly shared)

## 7) Quick verification before push

- `git status` clean except intended files
- open the pages locally and confirm:
  - sections render
  - links work
  - Text PJ still present + looks premium
