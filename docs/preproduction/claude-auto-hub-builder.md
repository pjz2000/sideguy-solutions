Claude,

SideGuy now supports automatic hub discovery.

Read:

docs/hub-discovery/auto-hub-discovery.md

Then run:

tools/hubs/discover-hubs.sh

From the results:

1. Identify topic clusters appearing in many pages.
2. Recommend potential knowledge hubs.
3. Build hubs if the cluster is strong.

Example hub format:

topic-knowledge-hub.html

Each hub should include:

hero explanation
key concepts
links to related pages
internal linking
Text PJ orb

Follow:

million-dollar-site-standard
clarity before cost tone

After building hubs:

add them to sitemap.xml
add them to index.html
link them from command center

Commit message:

Build: new knowledge hubs from auto-discovery
