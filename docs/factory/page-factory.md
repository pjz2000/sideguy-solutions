# SideGuy Page Factory

Purpose:

Convert manifest plans into real HTML pages.

Flow:

manifest queue
→ page factory
→ html output
→ internal linking
→ sitemap inclusion

Rules:

- append only
- never overwrite pages
- every page links to a hub
- clusters must exist first
